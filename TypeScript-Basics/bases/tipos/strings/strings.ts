(() => {
  const batman: string = "Bruce Wayne";
  const superman: string = "Clark Kent";
  const flash: string = `Barry Allen`;
  console.log(`${batman}" es Batman`);
  console.log(superman.concat(" es Superman").toLowerCase());
  console.log(flash.concat(" es Flash").toUpperCase());
})();
